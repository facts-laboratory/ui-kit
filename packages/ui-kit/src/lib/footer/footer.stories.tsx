import type { Meta } from '@storybook/react';
import { StandardFooter } from './footer';

const Story: Meta<typeof StandardFooter> = {
  component: StandardFooter,
  title: 'StandardFooter',
};
export default Story;

export const Primary = {
  args: {},
};
